name: Marcador de Teste do Vídeo V7

on:
  workflow_dispatch:

jobs:
  marcador:
    runs-on: ubuntu-latest
    steps:
      - run: echo "A validação real é executada pelo workflow protegido da branch principal."

name: Classificação Parcial ao Vivo do Brasileirão — DESATIVADA

# DESATIVADO por regra editorial do Portal SimonSports em 29/08/2026.
# Nenhuma classificação parcial deve ser publicada durante a Live.
# Mantido apenas como arquivo histórico; não possui gatilho automático nem manual.

on:
  workflow_dispatch:

jobs:
  classificacao-live-desativada:
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
      - name: Publicação parcial desativada
        run: echo "Classificação parcial desativada por regra editorial."
